import 'package:flutter/widgets.dart';

class ShareCellShowDetail extends StatelessWidget {
  ShareCellShowDetail({Key key, this.isShow}): super(key: key);
  final bool isShow;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
