import 'package:flutter/material.dart';

class PublicAccountPage extends StatelessWidget {
  const PublicAccountPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
