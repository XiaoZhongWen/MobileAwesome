import 'package:flutter/material.dart';

final roundBorderRadius = BorderRadius.circular(10);
final imageBorderRadius = BorderRadius.circular(5);
const topGreyBorder = Border(top: BorderSide(color: Colors.grey, width: 0.1));