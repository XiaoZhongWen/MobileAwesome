import 'package:flutter_cloud_platform/contacts/models/contacts_category.dart';

class ContactsDao {
  void saveContacts(ContactsCategoryItem item, {int? type, String? tag}) {
    // int? type = e?['type'] as int?;
    // String? tag = e?['tag'] as String?;
    // if (type != null) {
    //   List<dynamic>? items = e?['items'];
    //   items?.forEach((element) {
    //     Map<String, dynamic> json = Map.from(element);
    //     json['type'] = type;
    //     json['tag'] = tag;
    //
    //   });
    // }
    // Map<String, dynamic> record = {};
  }
}